*To-Do List App

A simple to-do list built with HTML, CSS, and vanilla JavaScript.

** Features
- Add new tasks
- Click a task to mark it complete (strikethrough)
- Delete tasks

** Files
- `index.html` — structure
- `style.css` — styling
- `script.js` — task logic (add / complete / delete)
